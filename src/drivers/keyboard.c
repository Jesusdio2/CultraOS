// src/drivers/keyboard.c
#include "keyboard.h"
#include "io.h"   // para inb/outb si ya lo tienes

void keyboard_init(void) {
    // Inicialización mínima del teclado (PS/2)
    // Por ahora puede estar vacío
}
